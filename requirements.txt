# ─────────────────────────────────────────────────────────────────────────────
# LOCKFILE — complete transitive pin of the runtime + dev dependency tree.
#
# GENERATED ON LINUX (ubuntu, Python 3.12 — the CI + EC2 deploy platform), so
# the pins are authoritative for where the code actually runs. Do not hand-edit.
# Regenerate after any pyproject.toml dependency change by running the `lockgen`
# GitHub workflow (or, equivalently, on an ubuntu/py3.12 host):
#
#     python3 -m venv /tmp/lock && /tmp/lock/bin/pip install -e ".[dev]"
#     /tmp/lock/bin/pip freeze --exclude-editable > requirements.txt
#     (then re-add this header)
#
# Direct dependencies and their version policy live in pyproject.toml — that is
# the source of truth for WHAT we depend on (and it PINS the web stack:
# fastapi==0.136.1 / starlette==1.0.0). This file locks WHICH transitive
# versions resolve, so CI, local, and the EC2 deploy install an identical tree.
#
# Consumed as a constraints file: `pip install -e ".[dev]" -c requirements.txt`
# (CI Test Suite + deploy/deploy.sh). The security scan installs it directly.
# Last regenerated: 2026-07-21 (Linux).
# ─────────────────────────────────────────────────────────────────────────────
aiomqtt==2.5.1
aiosqlite==0.22.1
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
APScheduler==3.11.3
asyncua==2.0.1
Authlib==1.7.2
bandit==1.9.4
boto3==1.43.52
botocore==1.43.52
certifi==2026.6.17
cffi==2.1.0
click==8.4.2
coverage==7.15.2
cryptography==49.0.0
defusedxml==0.7.1
Deprecated==1.3.1
dparse==0.6.4
fastapi==0.136.1
filelock==3.31.2
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
influxdb-client==1.50.0
iniconfig==2.3.0
Jinja2==3.1.6
jmespath==1.1.0
joblib==1.5.3
joserfc==1.7.4
limits==5.8.0
markdown-it-py==4.2.0
MarkupSafe==3.0.3
marshmallow==4.3.0
mdurl==0.1.2
narwhals==2.24.0
nltk==3.10.0
numpy==2.5.1
packaging==26.2
paho-mqtt==2.1.0
pandas==3.0.3
pluggy==1.6.0
pyasn1==0.6.4
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
Pygments==2.20.0
pymodbus==3.14.0
pyOpenSSL==26.3.0
pysnmp==7.1.27
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-multipart==0.0.32
pytz==2026.2
PyYAML==6.0.3
reactivex==5.0.0
regex==2026.7.19
rich==15.0.0
ruamel.yaml==0.19.1
ruff==0.15.22
s3transfer==0.19.1
safety==3.8.1
safety-schemas==0.0.16
scikit-learn==1.9.0
scipy==1.18.0
shellingham==1.5.4
six==1.17.0
slowapi==0.1.10
sortedcontainers==2.4.0
starlette==1.0.0
stevedore==5.9.0
structlog==26.1.0
tenacity==9.1.4
threadpoolctl==3.6.0
tomlkit==0.15.1
tqdm==4.69.0
truststore==0.10.4
typer==0.25.1
typing-inspection==0.4.2
typing_extensions==4.16.0
tzlocal==5.4.4
urllib3==2.7.0
uvicorn==0.51.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.1.1
wrapt==2.2.2
