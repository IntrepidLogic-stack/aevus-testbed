# Aevus Testbed — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# ── Data Collection ──
pysnmp>=6.2.0            # SNMP polling + trap receiver (Phase 1)
pymodbus>=3.7.0          # Modbus TCP (SCADAPack 470, port 502)
icmplib>=3.0.4           # Async ICMP probe (Phase 2 layer-3 reachability)
aiomqtt>=2.3.0           # Async MQTT publisher (Phase 4 IoT Core bridge)
# dnp3-python>=0.1.5     # DNP3 unsolicited responses (Phase 3 — patent path)
                          # Install manually on the Pi when the SCADAPack is
                          # online. The receiver module lazy-imports it so
                          # the rest of the platform installs and runs fine
                          # without it. See deploy/pi/README.md §5b.

# ── Storage ──
influxdb-client[async]>=1.46.0   # InfluxDB 2.x async client
aiosqlite>=0.20.0                # Async SQLite

# ── Scheduling ──
apscheduler>=3.10.0      # Polling job scheduler

# ── Processing / ML ──
numpy>=1.26.0
scikit-learn>=1.5.0      # Anomaly detection, prediction models
pandas>=2.2.0            # Time-series manipulation

# ── Logging ──
structlog>=24.4.0        # Structured JSON logging

# ── Dev / Test ──
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0            # Async test client for FastAPI
ruff>=0.8.0              # Linter
black>=24.10.0           # Formatter

# ── WebSocket ──
websockets>=14.0         # Already included via uvicorn[standard]
